{
  "name": "securepass-desktop",
  "version": "1.0.0",
  "description": "SecurePass - Minimalist Zero-Knowledge Windows Password Manager & ML Security Analyzer",
  "main": "main.js",
  "author": "Shivang",
  "license": "MIT",
  "scripts": {
    "start": "electron .",
    "check": "node -c crypto-vault.js && node -c main.js && node -c preload.js && node -c renderer/analyzer-engine.js && node -c renderer/renderer.js",
    "test": "npm run check && node tests/crypto-vault.test.js && node tests/analyzer.test.js && node tests/totp.test.js",
    "pack:win": "electron-builder --win --dir",
    "dist:win": "electron-builder --win"
  },
  "devDependencies": {
    "electron": "^34.3.0",
    "electron-builder": "^25.1.8"
  },
  "build": {
    "appId": "com.shivang.securepass",
    "productName": "SecurePass",
    "directories": {
      "output": "dist"
    },
    "win": {
      "sign": null,
      "target": [
        "nsis",
        "portable"
      ]
    },
    "nsis": {
      "oneClick": false,
      "perMachine": false,
      "allowToChangeInstallationDirectory": true,
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true,
      "shortcutName": "SecurePass",
      "artifactName": "SecurePass-Setup-${version}.exe"
    },
    "portable": {
      "artifactName": "SecurePass-${version}-Portable.exe"
    },
    "files": [
      "**/*",
      "!dist/**/*"
    ]
  }
}
